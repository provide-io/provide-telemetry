{
  "$schema": "https://raw.githubusercontent.com/stryker-mutator/stryker-net/master/src/Stryker.CLI/Stryker.CLI/schema.json",
  "//": [
    "JSON has no comment syntax, so the rationale lives in this ignored key.",
    "Stryker reads only the 'stryker-config' node; everything here is inert.",
    "",
    "SCOPE. Solution mode is used rather than a single -p project because two",
    "projects are owned and both must be mutated. Stryker mutates exactly the",
    "projects referenced by the test projects listed below -- Provide.Telemetry",
    "and Provide.Telemetry.OpenTelemetry. The probes, examples and consumer",
    "smoke projects are in the solution but no test project references them, so",
    "they are never mutated and need no exclusion entry.",
    "",
    "MEASURED BASELINE (2026-08-09, Stryker.NET 4.16.0, .NET SDK 10.0.302):",
    "  2172 mutants created; 390 ignored (block already covered), 104 compile",
    "  error, leaving 1678 scored.",
    "  killed 1337 | survived 309 | timeout 7 | no-coverage 25",
    "  final mutation score 80.10 %  (34m18s wall clock at concurrency 2)",
    "Split by project: core 82.0 % (1227 killed / 1504 scored),",
    "OpenTelemetry 63.2 % (110 killed / 174 scored).",
    "Previous measurement was 79.14 % (killed 1318, survived 325). The 16",
    "survivors closed were three clusters that each hid a real gap rather than",
    "an equivalent mutant: the three ValidateEndpoint calls in ConfigFromEnv",
    "(driven one signal at a time through OTEL_EXPORTER_OTLP_<SIGNAL>_ENDPOINT,",
    "because the shared variable lets any two of them cover for a missing",
    "third), the hardening depth ceiling and all seven of its per-container",
    "depth increments, and the Logs arm of the exporter-retries ceiling.",
    "",
    "COMPILE ERRORS. Was 146, of which 24 were in Setup.cs: assigning a local",
    "inside a try/catch or a lock left it only conditionally assigned, so any",
    "mutant of that assignment raised CS0165 and Safe Mode discarded every",
    "mutant in SetupTelemetry -- the entry point was unmeasured, not passing.",
    "Those locals are now definitely assigned at their declaration or hoisted",
    "into a method that returns, and Setup.cs is down to 1 compile error with 15",
    "more mutants scored. The remaining 104 are a different animal and cannot be",
    "restructured away: Stryker emits type-invalid code for them. 23 of the 25",
    "in CanonicalJson.cs are the Linq mutator rewriting StringBuilder.Append to",
    "Prepend, which StringBuilder does not have; Pii.cs's are Count() to Sum()",
    "over non-numeric sequences and equality mutants on IReadOnlyList. They are",
    "mutants that never existed rather than mutants being hidden -- CanonicalJson",
    "still scores 100 killed of 107, and Pii 98 of 110.",
    "",
    "THRESHOLD. break is 85, deliberately below the 86.50 % measured",
    "(2026-08-16, 1611 killed / 240 survived / 23 timeout / 15 no-coverage of",
    "1889 scored -- the span-redaction change moved the mutant population), not",
    "at 100. A threshold above the real score is a gate that fails on green and",
    "gets disabled; the ~1.8pp of slack (about 33 mutants) covers roughly twice",
    "the timeout band -- 16-21 mutants whose kill/timeout classification",
    "depends on host load, widened by the wire tests' real HTTP latency. Ratchet break upward as survivors are killed, never",
    "down. History: 79.41 -> 79.14 (Setup.cs mutants that used to be discarded",
    "became countable) -> 80.10 (endpoint, hardening-depth and retries-ceiling",
    "clusters) -> 85.84 (config/DTO/redaction literal sweep, governance table",
    "and consent env name, receipt timestamp format, MapLevel bridge map,",
    "escape-class boundaries, BuildSignalUri host/port shapes, plus the",
    "breaker's Resilience.Clock seam taking Resilience.cs itself to zero",
    "survivors; four equivalent mutants were removed structurally --",
    "ValidateEndpoint's empty fast path, ParseHeaders' split options and eq",
    "boundary, RecordSuccess's return -- rather than suppressed) -> 86.81",
    "(FakeOtlpCollector: per-signal paths, headers, exporter option booleans,",
    "SLO names and the shutdown drain became wire-assertable).",
    "",
    "RAISING IT FURTHER. What remains is mostly observable only on the OTLP",
    "wire or in states tests cannot reach: OpenTelemetryBackend.cs (49) is the",
    "exporter-lambda path literals, the DI drain arms and the teardown Swallow",
    "statements, none of which the SDK exposes after Build(); Setup.cs (33) is",
    "backend-registration wiring of the same character; Governance.cs (15) is",
    "the SLO metric names, unreadable in fallback mode because Metrics.Counter",
    "mints a fresh instrument per call; Hardening.cs (14) is the IsComposite",
    "pattern list that only gates cycle tracking. ConfigureAwait mutants and",
    "comparisons whose sides agree on every reachable value stay equivalent.",
    "Moving past ~90 means making the wire observable -- a fake OTLP collector",
    "in-process -- not more assertions on the current surfaces."
  ],
  "stryker-config": {
    "solution": "Provide.Telemetry.sln",
    "test-projects": [
      "tests/Provide.Telemetry.Tests/Provide.Telemetry.Tests.csproj",
      "tests/Provide.Telemetry.OpenTelemetry.Tests/Provide.Telemetry.OpenTelemetry.Tests.csproj"
    ],
    "mutate": [
      "**/*.cs",
      "!**/obj/**/*.cs",
      "!**/bin/**/*.cs"
    ],
    "concurrency": 2,
    "reporters": [
      "progress",
      "html",
      "json"
    ],
    "thresholds": {
      "high": 90,
      "low": 86,
      "break": 85
    }
  }
}
